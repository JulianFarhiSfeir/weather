import { ConditionTabLabelPipe } from './condition-tab-label.pipe';

describe('ConditionTabLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionTabLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
